# Optional — without this the Council still runs end-to-end on a rule-based
# simulation with the identical output contract. Set it to get live Claude
# reasoning from the five specialists and the Chair.
ANTHROPIC_API_KEY=

# Defaults shown; override if needed.
ANTHROPIC_MODEL=claude-sonnet-5
AUTO_EXECUTE_THRESHOLD=0.9
PORT=8787
